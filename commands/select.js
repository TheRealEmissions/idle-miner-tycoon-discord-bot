module.exports = class select {
    constructor() {
        this.name = 'select',
        this.alias = ['sel'],
        this.usage = '#select <mine number>'
    }

    async run(client, message, args) {
        
    }
}